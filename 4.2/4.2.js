let rand10 = () => Math.ceil(Math.random() * 10);
alert(rand10());
// Je crée une fonction qui retourne un chiffre aléatoire entre 1 et 10
// J'utilise Math.random pour générer un chiffre aléatoire entre 0 et 1
// J'utilise Math.ceil pour faire en sorte que ce chiffre soit entier